{
    "name": "boilerplate-rails",
    "version": "1.0.0",
    "scripts": {
        "heroku-postbuild": "cd reactapp && npm install && npm run build && mv build/* ../public/"
    },
    "engines": {
        "node": "14.x"
    },
    "dependencies": {
        "@stripe/react-stripe-js": "^2.7.1",
        "@stripe/stripe-js": "^3.4.1",
        "react-helmet": "^6.1.0"
    }
}
